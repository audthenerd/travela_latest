<p id="notice"><%= notice %></p>


<p>
  <strong>Title:</strong>
  <%= @place.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @place.content %>
</p>

<%= link_to 'Edit', edit_trip_place_path(@trip, @place) %> |
<%= link_to 'Back', trip_places_path %>
